---
type Props = {
	img: string;
	title: string;
};

const { img, title } = Astro.props;
---

<div class="space-y-3">
	<img src={img} alt="" width={1340} height={950} />
	<p class="text-center text-[21px] whitespace-pre-line lg:text-left">
		{title}
	</p>
</div>
